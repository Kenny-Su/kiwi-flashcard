# CloudFront forwards the viewer's Host header, so Caddy must serve both names.
flashcard.apps-server.kiwiai.org, flashcard.apps.kiwiai.org {
	tls {
		dns route53 {
			hosted_zone_id {$AWS_HOSTED_ZONE_ID}
		}
	}

	reverse_proxy app:8002
}
